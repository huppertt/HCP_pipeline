
for i=1:length(ff)
    cd('/disk/sulcus1/COBRA');
    [p,f,e]=fileparts(ff(i).nane);
    system(['mkdir -p ' p '/mcf']);
    cd([ p '/mcf']);
    if(~exist([f '_mcf.par']))
        system(['mcflirt -in ../' f e ' -report -plots -stats'])
    else
        disp(['skipping ' f]);
    end
end